(library
 (name grace_lib)
 ;; (modules (:standard \ codegen))
 (libraries llvm llvm.analysis llvm.all_backends llvm.target))

(ocamllex
 (modules lexer))

(menhir
 (modules tokens)
 (flags --only-tokens))

(menhir
 (modules parser tokens)
 (merge_into parser)
 (flags --external-tokens Tokens))

(env
 (dev
  (flags
   (:standard -warn-error -A))))
