; ModuleID = 'grace'
source_filename = "grace"
target datalayout = "e-m:e-p270:32:32-p271:32:32-p272:64:64-i64:64-f80:128-n8:16:32:64-S128"
target triple = "x86_64-pc-linux-gnu"

%frame___start = type {}
%frame___start.doSomething = type { %frame___start* }

@"Hello world\0A\0A" = private unnamed_addr constant [14 x i8] c"Hello world\0A\0A\00", align 1

declare void @writeInteger(i32)

declare void @writeChar(i8)

declare void @writeString(i8*)

declare i32 @readInteger()

declare i8 @readChar()

declare void @readString(i32, i8*)

declare i32 @ascii(i8)

declare i8 @chr(i32)

declare i32 @strlen(i8*)

declare i32 @strcmp(i8*, i8*)

declare void @strcpy(i8*, i8*)

declare void @strcat(i8*, i8*)

define void @_start.doSomething(%frame___start* %0) {
entry:
  %frame_struct = alloca %frame___start.doSomething, align 8
  %param_ptr = getelementptr inbounds %frame___start.doSomething, %frame___start.doSomething* %frame_struct, i32 0, i32 0
  store %frame___start* %0, %frame___start** %param_ptr, align 8
  call void @writeString(i8* getelementptr inbounds ([14 x i8], [14 x i8]* @"Hello world\0A\0A", i32 0, i32 0))
  ret void
}

define i32 @_start() {
entry:
  %frame_struct = alloca %frame___start, align 8
  call void @_start.doSomething(%frame___start* %frame_struct)
  ret i32 0
}
