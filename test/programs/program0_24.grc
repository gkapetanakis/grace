fun doSomething() : nothing
  var c: char;
  var i: int;
  var s: char[10];
{
  c <- readChar();
  i <- readInteger();
  readString(10, s);

  writeChar(c);
  writeChar('\n');
  writeInteger(i);
  writeChar('\n');
  writeString(s);
  writeChar('\n');
}
